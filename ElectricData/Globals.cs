namespace ElectricData
{
    //Глобальные переменные
    public class Globals
    {
        public static string SettingsFile = "settings.xml"; //Название файла сохранения настроек подключения (имени сервера и имени базы данных)
        public static bool exit_app = false; //Рестарт приложения
    }
}
